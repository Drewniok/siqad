28th of March:

First, go to plugins/simmaneal and run the following commands:

1.) pip install scikit-build
2.) python3 setup.py build

Afterwards, you find pysimmanneal in the _skbuild folder. Copy the pysimmanneal folder to the src/analysis_tool/tts/
